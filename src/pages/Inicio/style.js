import styled from 'styled-components';

export const ButtonPointRegister = styled.div`
    font-size: 1.2em; 
    font-weight: normal; 
    background-color: var(--color-primary); 
    color: white; 
    padding: 0.8rem; 
    width: 17%;
    border-radius: 1.8rem;
    cursor: pointer;
`
